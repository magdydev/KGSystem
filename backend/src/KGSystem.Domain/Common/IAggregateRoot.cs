namespace KGSystem.Domain.Common;

/// <summary>
/// Marker interface for aggregate roots — the only entities repositories
/// are allowed to load and persist directly.
/// </summary>
public interface IAggregateRoot
{
}
